var a : String;
var items : Sequence<Integer>;

for (item in items) {
	a.asUni/*@asUnicode*/
	//@ Any, Bag, Boolean, Collection, ConcurrentBag, ConcurrentMap, ConcurrentSet, Integer, List, Map, Native, None, Nothing, OrderedSet, Real, Sequence, Set, String, Tuple, a, hasMore, item, items, loopCount
	var local : Integer = 0;
	//@ Any, Bag, Boolean, Collection, ConcurrentBag, ConcurrentMap, ConcurrentSet, Integer, List, Map, Native, None, Nothing, OrderedSet, Real, Sequence, Set, String, Tuple, a, hasMore, item, items, local, loopCount
	a.asUni/*@asUnicode*/
}
