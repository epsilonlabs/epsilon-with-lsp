operation String join(prefix : String, suffix : String) : String {
	prefix.println(); //@ prefix, self, suffix
	var combined = prefix + suffix;
	combined.println(); //@ combined, prefix, self, suffix
	return combined;
}
